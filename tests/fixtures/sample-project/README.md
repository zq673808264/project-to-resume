# AI Notes Assistant

AI Notes Assistant is a small Python CLI tool that summarizes project notes with an LLM-style workflow.

It reads Markdown notes, extracts action items, and writes a structured summary for developers.
